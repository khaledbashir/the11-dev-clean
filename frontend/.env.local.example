# Example environment for local frontend development (using remote Easypanel backend)
# ---------------------------------------------------------
# IMPORTANT:
#  - Copy this file to frontend/.env.local (NOT committed) and fill secrets.
#  - This example points the frontend to the remote Easypanel backend (ahmad/socialgarden-backend).
#  - Do NOT commit private API keys or secrets into source control.
#  - Restart the frontend dev server after you update .env.local (pnpm dev).
#  - If you want to use the remote backend but still run local backend, remove or set USE_REMOTE_BACKEND=0.
# ---------------------------------------------------------

# FRONTEND (app router) / Browser-accessible settings (prefix with NEXT_PUBLIC_)
# These control what the frontend will call for backend API requests and PDF generation.
NEXT_PUBLIC_PDF_SERVICE_URL=https://ahmad-socialgarden-backend.840tjq.easypanel.host
NEXT_PUBLIC_BACKEND_URL=https://ahmad-socialgarden-backend.840tjq.easypanel.host
NEXT_PUBLIC_API_URL=https://ahmad-socialgarden-backend.840tjq.easypanel.host

# Keep this set to your local frontend dev address
NEXT_PUBLIC_BASE_URL=http://localhost:3333
FRONTEND_PORT=3333

# AnythingLLM (public or browser-exposed)
NEXT_PUBLIC_ANYTHINGLLM_URL=https://ahmad-anything-llm.840tjq.easypanel.host
# Placeholder — replace with your key if needed locally, DO NOT commit this
NEXT_PUBLIC_ANYTHINGLLM_API_KEY=your_anythingllm_api_key_here

# SERVER-SIDE (not exposed to browser) — used by the Next server, API routes or scripts
# Use these to call the remote backend or services safely from server-side code.
PDF_SERVICE_URL=https://ahmad-socialgarden-backend.840tjq.easypanel.host
ANYTHINGLLM_URL=https://ahmad-anything-llm.840tjq.easypanel.host
ANYTHINGLLM_API_KEY=your_anythingllm_api_key_here

# If you need Google OAuth for local testing (Optional)
# You should set up dev OAuth credentials and set redirect URIs to http://localhost:3333/api/oauth/callback
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3333/api/oauth/callback

# OpenRouter / Inline editor (Optional)
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL_PREF=moonshotai/kimi-k2-instruct

# LOCAL DATABASE (Optional — only if your frontend server/API routes connect directly to DB)
# CAUTION: Connecting a local frontend to the remote DB is potentially dangerous and slow.
# Leave these blank if you plan to route everything through your remote backend.
DB_HOST=168.231.115.219
DB_PORT=3306
DB_NAME=socialgarden_sow
DB_USER=sg_sow_user
DB_PASSWORD=your_db_password_here

# Developer helper flags (used by dev.sh)
# Set USE_REMOTE_BACKEND=1 to skip starting the local backend when running ./dev.sh
# When USE_REMOTE_BACKEND=1, the script sets the frontend env to use the remote backend described above.
USE_REMOTE_BACKEND=1

# Optional: Additional debugging / runtime settings
# NODE_ENV=development
# DEBUG=app:*

# Notes:
# 1) If the browser returns CORS errors, add `http://localhost:3333` (or the port you're running on)
#    to the remote backend's CORS configuration.
# 2) If you want to test ONLY against the remote backend and not use a remote DB, omit DB_*
# 3) Keep production values / keys secure. Use a real secret store for production deployments
# 4) When deploying to Easypanel, these values are typically set in the panel's Environment UI.
# ---------------------------------------------------------
# End of example env

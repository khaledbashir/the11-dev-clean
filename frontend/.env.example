# ==============================================================================
# THE11 SOW GENERATOR - ENVIRONMENT VARIABLES
# ==============================================================================
# This file documents all environment variables used by the application.
# Copy this to .env and fill in your actual values.
# Variables marked REQUIRED must be set for the application to function.
# ==============================================================================

# ------------------------------------------------------------------------------
# DATABASE CONFIGURATION (REQUIRED)
# ------------------------------------------------------------------------------
# MySQL database connection for storing SOWs, folders, and user data
DB_HOST=localhost                           # Database hostname
DB_USER=your_db_user                       # Database username
DB_PASSWORD=your_secure_password_here      # Database password
DB_NAME=socialgarden_sow                   # Database name
DB_PORT=3306                               # MySQL port (default: 3306)

# ------------------------------------------------------------------------------
# ANYTHINGLLM INTEGRATION (REQUIRED FOR AI FEATURES)
# ------------------------------------------------------------------------------
# AnythingLLM is our unified AI platform - handles ALL AI features:
# - SOW generation (The Architect)
# - Dashboard analytics (Analytics Assistant)
# - Prompt enhancement (utility-prompt-enhancer workspace) → ✨ Enhance button
# - Inline editing (utility-inline-editor workspace) → Selection highlight + /ai
#
# Get your AnythingLLM instance at: https://anythingllm.com
# Create an API key in: Settings → API Keys

# Server-side AnythingLLM config (preferred for security)
ANYTHINGLLM_URL=https://your-anythingllm-instance.com
ANYTHINGLLM_API_KEY=your_anythingllm_api_key_here

# Client-side AnythingLLM config (used in browser where needed)
NEXT_PUBLIC_ANYTHINGLLM_URL=https://your-anythingllm-instance.com
NEXT_PUBLIC_ANYTHINGLLM_API_KEY=your_anythingllm_api_key_here

# OPTIONAL: Override default workspace slugs for AI features
# Prompt enhancer (✨ Enhance button on workspace creation)
# Default: utility-prompt-enhancer
# PROMPT_ENHANCER_WORKSPACE=utility-prompt-enhancer

# Inline editor (selection highlight + slash /ai command)
# Default: utility-inline-editor (falls back to 'pop' if not found)
# INLINE_EDITOR_WORKSPACE=utility-inline-editor

# ------------------------------------------------------------------------------
# DEPRECATED AI KEYS (NO LONGER USED)
# ------------------------------------------------------------------------------
# These were used before AI unification (Oct 2025). Now ALL AI goes through
# AnythingLLM workspaces. Kept for documentation purposes only.
# OpenRouter: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Groq (fallback): https://console.groq.com/keys
GROQ_API_KEY=

# OpenAI (direct): https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_BASE_URL=

# ------------------------------------------------------------------------------
# APPLICATION URLS (REQUIRED)
# ------------------------------------------------------------------------------
# Public-facing URL where your app is hosted
NEXT_PUBLIC_BASE_URL=http://localhost:3001

# API base URL (usually same as base URL for Next.js)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Backend FastAPI URL (if using separate backend server)
# Default: http://localhost:8000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# PDF service URL (for SOW exports)
# Default: http://localhost:8000
NEXT_PUBLIC_PDF_SERVICE_URL=http://localhost:8000

# ------------------------------------------------------------------------------
# GOOGLE OAUTH (REQUIRED FOR GOOGLE SHEETS INTEGRATION)
# ------------------------------------------------------------------------------
# Enable "Create GSheet" feature for exporting SOWs to Google Sheets
# Get credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# OAuth redirect URI (must match Google Console settings)
GOOGLE_REDIRECT_URI=http://localhost:3001/api/oauth/callback

# ------------------------------------------------------------------------------
# OPTIONAL: VERCEL SERVICES
# ------------------------------------------------------------------------------
# Vercel Blob Storage (for image uploads in editor)
# Get credentials: https://vercel.com/docs/storage/vercel-blob/quickstart
BLOB_READ_WRITE_TOKEN=

# Vercel KV (for API rate limiting)
# Get credentials: https://vercel.com/docs/storage/vercel-kv/quickstart
KV_REST_API_URL=
KV_REST_API_TOKEN=

# ------------------------------------------------------------------------------
# OPTIONAL: ANALYTICS & MONITORING
# ------------------------------------------------------------------------------
# Plausible Analytics (privacy-friendly analytics)
# Self-hosted instance or cloud: https://plausible.io
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=your-domain.com
NEXT_PUBLIC_PLAUSIBLE_URL=https://your-plausible-instance.com

# ------------------------------------------------------------------------------
# DEVELOPMENT & DEBUGGING
# ------------------------------------------------------------------------------
# Node environment (development | production | test)
NODE_ENV=development

# Enable verbose logging for debugging
# DEBUG=true

# ==============================================================================
# SETUP CHECKLIST
# ==============================================================================
# [ ] Copy this file to .env
# [ ] Set all REQUIRED variables (Database, AnythingLLM, URLs)
# [ ] Create AnythingLLM workspaces (see: /00-ANYTHINGLLM-WORKSPACE-SETUP.md):
#     - utility-prompt-enhancer (for ✨ Enhance button on workspace creation)
#     - utility-inline-editor (for selection highlight + /ai slash command)
# [ ] Optional: Set up Google OAuth for GSheets export
# [ ] Optional: Configure Vercel services if using image uploads
# [ ] Run: npm install
# [ ] Run: npm run dev
# [ ] Test: Visit http://localhost:3001
# ==============================================================================
